---
layout: base.njk
title: "Water Treatment, Boreholes & Legionella Control | Invicta Water Treatment"
description: "Bespoke water treatment, chlorination and hygiene compliance across Surrey, Sussex and the South East. CHAS accredited, UKAS testing, 24-hour callout. Twenty years' experience."
canonical: "https://www.invictawatertreatment.co.uk/"
permalink: "/"
current: "home"
extraStyles: ["homepage", "filmstrip", "coverage-band"]
hideFooterCta: true
---
    {#
      ======================================================================
      HOME PAGE

      NINE SECTIONS, AND NO TWO OF THEM ARE BUILT THE SAME WAY. The page this
      replaces was five variations on "eyebrow, big heading, grid of cards",
      which made every screen read like the one before it. Each section here
      has one job, and a structure only it uses. The reasoning for each is in
      homepage.css, next to the rules that draw it, along with the argument for
      the running order.

      NO SECTORS SECTION, AND FOUR SERVICE ROUTES RATHER THAN FIVE. Both are
      the client's own instruction of 11/08/26: "Remove Sectors completely" and
      "Services, remove Flush & Precomm". Those pages 301 in src/_redirects, so
      a card offering pre-commission cleaning would have sent the one visitor
      who wanted it to a page about boreholes. The coverage map now stands
      where the sectors list did, answering "do you come to me" directly.

      ROUTING SITS THIRD, one screen from the fold, because on a page selling
      systems that cost seven figures the visitor almost always arrives already
      knowing something has to be sorted. The positioning statement reads
      better after the five questions than in front of them: by then it is
      answering "why does this have to be done properly" rather than opening
      with it.

      THREE OF THE NINE ARE THE SITE'S OWN COMPONENTS, unchanged and included
      rather than reimplemented: the coverage map that closes /about/ and every
      service page, the filmstrip from /about/, and the enquiry drawer the
      whole site shares. A homepage that drew its own map or its own photo band
      would be a second answer to a question the site already answers once.

      THERE IS NO DEDICATED EMERGENCY SECTION, and that is deliberate. The
      number is already persistent on every screen: the sticky bar carries it
      above 900px, the floating dock carries it below, the hero states it in
      full, and the footer repeats it. An 88px billboard of it in the middle of
      the page was the one block here saying a fifth time what nothing on the
      page lets you miss.

      THE FOOTER CTA IS TURNED OFF ON THIS PAGE (hideFooterCta above), and
      that is not an oversight: section 9 IS that call to action, word for
      word, and with both on the page the same heading and the same paragraph
      appeared twice within one screen of each other. The footer itself is
      untouched.

      THE TOOLTIP COMPONENT is site-wide and NOTHING IS MARKED UP HERE. The
      eight hand-written `data-tooltip` spans this page used to carry are gone:
      the `gloss` filter in .eleventy.js now wraps the first occurrence of every
      term in src/_data/glossary.js, on every page, and enforces the same rules
      by machine (never in a heading, never inside a link, two per paragraph,
      once per page). Two of the hand-written ones sat inside a card <a>, which
      the rules said not to do and only a person could miss. To gloss a new
      word, add it to the glossary; to change a definition, change it there.
      ======================================================================
    #}

    {#
      Organization + LocalBusiness, IN THE BODY RATHER THAN THE HEAD, because
      base.njk owns <head> and is shared by every page. JSON-LD is valid and
      indexed anywhere in the document, so the placement costs nothing.

      FOLLOW-UP: base.njk already emits a minimal LocalBusiness on every page.
      Both nodes describe the same business, so give that one the same @id as
      the node below and the two merge into one instead of reading as two
      businesses at one address. That edit belongs to a shared-layout task,
      not to this page.
    #}
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@graph": [
        {
          "@type": "Organization",
          "@id": "{{ site.url }}/#organization",
          "name": "{{ site.name }}",
          "legalName": "{{ site.legalName }}",
          "url": "{{ site.url }}/",
          "telephone": "{{ site.tel }}",
          "email": "{{ site.email }}",
          "description": "Bespoke water treatment, private water supplies, chlorination and water hygiene compliance for estates, plant and commercial facilities across Surrey, Sussex and the South East.",
          "address": {
            "@type": "PostalAddress",
            "streetAddress": "{{ site.address.street }}",
            "addressLocality": "{{ site.address.locality }}",
            "addressRegion": "{{ site.address.region }}",
            "postalCode": "{{ site.address.postcode }}",
            "addressCountry": "GB"
          }
        },
        {
          "@type": "LocalBusiness",
          "@id": "{{ site.url }}/#localbusiness",
          "parentOrganization": { "@id": "{{ site.url }}/#organization" },
          "name": "{{ site.name }}",
          "url": "{{ site.url }}/",
          "telephone": "{{ site.tel }}",
          "email": "{{ site.email }}",
          "priceRange": "££",
          "address": {
            "@type": "PostalAddress",
            "streetAddress": "{{ site.address.street }}",
            "addressLocality": "{{ site.address.locality }}",
            "addressRegion": "{{ site.address.region }}",
            "postalCode": "{{ site.address.postcode }}",
            "addressCountry": "GB"
          },
          "areaServed": [{% for c in maps.counties %}{ "@type": "AdministrativeArea", "name": "{{ c }}" }{% if not loop.last %}, {% endif %}{% endfor %}]
        }
      ]
    }
    </script>

    {# ===================== 1 — HERO =====================
      Full screen, left-aligned, and only two things to do: book the survey, or
      call the emergency line. The photograph is the site's existing hero image
      held right back under two washes, so the first screen is TYPE with a
      place behind it rather than a picture with type on it.

      The sticky emergency bar and nav above are untouched, so the number stays
      in view for the whole page; the outlined button here is the same number
      said once at full size, for someone who has landed mid-crisis.
    #}
    <section class="section hp-hero" id="hero" aria-labelledby="hp-hero-title">
      <div class="hp-hero__media" aria-hidden="true">
        <img src="/assets/estate-dawn.webp" alt="" fetchpriority="high" decoding="async">
      </div>
      <div class="container">
        <h1 class="hp-hero__title" id="hp-hero-title">Water systems you can <span class="em">depend</span> on.</h1>
        <p class="hp-hero__lead">Bespoke treatment, chlorination and hygiene for properties where getting it right is a legal requirement. Designed, installed and maintained personally, across Surrey, Sussex and the South East.</p>
        <div class="hp-hero__actions">
          <a class="btn-inquiry btn-inquiry--lg" href="/enquiry/">Book a site survey <span class="btn-inquiry__arrow" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg></span></a>
          <a class="btn-line btn-line--lg" href="tel:{{ site.tel }}" aria-label="Call the 24-hour emergency line on {{ site.phone }}">24hr emergency: {{ site.phone }}</a>
        </div>
      </div>
    </section>

    {# ===================== 2 — TRUST STRIP =====================
      A BAND, NOT A SECTION: 48px of height, a label, and the marks. It is
      evidence in passing, placed immediately under the claim it supports, and
      it is deliberately the shortest thing on the page.

      LOGO FILES TO COME FROM MATT. The four client names below are the ones
      already named on the live site, held as placeholder slots; drop the real
      file into each slot as:

          <li class="hp-trust__logo"><img src="/assets/logo-chelsea-fc.svg" alt="Chelsea FC" width="120" height="34"></li>

      and delete the --ph class and the text. The grayscale + opacity
      treatment lives on .hp-trust__logo, so it carries over to the real files
      with no further change.

      Alpha, Carel, DIS, IAC and SE Water are NOT slotted below. They were
      listed "if licensed", and a client mark goes up when we know we may use
      it, not before. Add a slot each once that is confirmed.

      Accreditation marks (CHAS, UKAS, and any others) are the second row, and
      are also awaiting files from Matt.
    #}
    <section class="section section--light hp-intro" id="about-invicta" aria-labelledby="hp-intro-title">
      <div class="container hp-intro__inner">
        {# An h2 wearing the eyebrow style. The section needs an accessible name
           and this is the only short string in it: the paragraph below runs to
           two sentences, and a 50-word heading is a heading in name only. #}
        <h2 class="eyebrow hp-intro__eyebrow" id="hp-intro-title">Who we are</h2>

        <p class="hp-intro__lead">Invicta is run, day to day, by <strong>Stefan Massingham</strong>, a water chemist of thirty years, and every problem that reaches us is reviewed by him personally. We take care of the science and the compliance, so you are left with a supply you never have to think about.</p>

        <a class="btn-line hp-intro__more" href="/about/">More about Invicta <span class="btn-line__arrow" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M9 6l6 6-6 6"/></svg></span></a>

        {# THE PROOF SITS UNDER THE CLAIM, in the same centred column, rather than
           in a band of its own. It was its own light section directly below this
           one, which put two light strips back to back with a full section step
           of empty cream between them and made the logos read as an orphan.

           Alpha, Carel, DIS, IAC and SE Water are NOT slotted below. They were
           listed "if licensed", and a client mark goes up when we know we may
           use it, not before. Add a slot each once that is confirmed.
           Accreditation marks are the second row, also awaiting files. #}
        <div class="hp-intro__trust">
          <div class="hp-trust__row hp-trust__row--centred">
            <p class="eyebrow hp-trust__label">Trusted by</p>
            <ul class="hp-trust__logos" role="list" data-no-gloss><!-- logo stand-ins, not prose: no tooltips here -->
              <li class="hp-trust__logo hp-trust__logo--ph">Chelsea FC</li>
              <li class="hp-trust__logo hp-trust__logo--ph">GSK</li>
              <li class="hp-trust__logo hp-trust__logo--ph">Amgen</li>
              <li class="hp-trust__logo hp-trust__logo--ph">Cowdray Estate</li>
            </ul>
          </div>
          <div class="hp-trust__row hp-trust__row--centred">
            <p class="eyebrow hp-trust__label">Accredited by</p>
            <ul class="hp-trust__logos" role="list" data-no-gloss><!-- logo stand-ins, not prose: no tooltips here -->
              <li class="hp-trust__logo hp-trust__logo--ph">CHAS</li>
              <li class="hp-trust__logo hp-trust__logo--ph">UKAS</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    {# ===================== 3 — SERVICES =====================
      ROUTED BY PROBLEM, NOT LISTED BY SERVICE. Every heading here is the
      question the visitor arrived with, because someone who has just been
      served a notice does not search for "water hygiene", they search for what
      has gone wrong. The service name is on the page they land on.

      Set as an asymmetric ruled spread rather than a card grid: five equal
      tiles would say these are five equivalent choices, and they are not.
    #}
    <section class="section hp-services" id="services" aria-labelledby="hp-services-title">
      <div class="container">
        <p class="eyebrow hp-eyebrow"><span class="hp-eyebrow__num">01</span>Services</p>
        <h2 class="statement hp-services__title" id="hp-services-title">What do you need to sort?</h2>

        <div class="hp-services__grid">
          {# THE FLAGSHIP CARRIES THE PHOTOGRAPH, and the four beside it do not.
             Five pictures in a routing grid is five things to look at before
             you can read a single question, and the questions are the point.
             One picture on the cell that already spans two rows is what makes
             the spread read as a magazine page with a lead image rather than
             as a tile grid, and it says which of the five is the flagship
             without a word of copy. Same frame as every other photograph on
             the site: rounded, clipped, drifting on scroll. #}
          <a class="hp-svc" href="/services/private-water-supplies/">
            <span class="hp-svc__media parallax">
              <img src="/assets/step-survey.webp" alt="A country house and garden buildings across a lawn on a private estate, the kind of property surveyed before a supply is specified." width="1200" height="961" loading="lazy" decoding="async">
            </span>
            <h3 class="hp-svc__title">Off the mains, or wish you were?</h3>
            <p class="hp-svc__desc">Private water supplies designed around your source, built to run for decades.</p>
            <span class="hp-svc__go">Private water supplies <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6"/></svg></span>
          </a>

          <a class="hp-svc" href="/services/design-install/">
            <h3 class="hp-svc__title">Building a new system?</h3>
            <p class="hp-svc__desc">Full design and install by our own engineers, specified to comply from day one.</p>
            <span class="hp-svc__go">Design and install <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6"/></svg></span>
          </a>

          <a class="hp-svc" href="/services/water-hygiene/">
            <h3 class="hp-svc__title">Legionella and hygiene to keep on top of?</h3>
            <p class="hp-svc__desc">Legal compliance is not negotiable. We handle water hygiene, sampling, temperature monitoring and Legionella control across your estate or portfolio.</p>
            <span class="hp-svc__go">Water hygiene <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6"/></svg></span>
          </a>

          <a class="hp-svc" href="/services/servicing/">
            <h3 class="hp-svc__title">System already installed?</h3>
            <p class="hp-svc__desc">Scheduled servicing, UKAS-accredited re-testing, and 24-hour callout for existing clients.</p>
            <span class="hp-svc__go">Servicing and maintenance <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M13 6l6 6-6 6"/></svg></span>
          </a>
        </div>
      </div>
    </section>

    {# ===================== 4 — POSITIONING =====================
      One line, centred, with nothing to click. This is the only section on the
      page with no next step in it, which is the point: it is the argument, and
      an argument that ends in a button reads as a pitch.
    #}
    <section class="section section--paper hp-position" id="positioning" aria-labelledby="hp-position-line">
      <div class="container">
        <div class="hp-position__inner">
          <h2 class="statement hp-position__line" id="hp-position-line">Water treatment isn't optional. <span class="em">Neither is doing it properly.</span></h2>
          <p class="hp-position__body">Twenty years of bespoke systems for clients who need current compliance evidence, not just clean water. Chelsea FC, GSK, Amgen, Cowdray Estate. The standard we work to is the standard your inspector expects.</p>
        </div>
      </div>
    </section>

    {# ===================== 5 — COVERAGE =====================
      WHERE WE WORK, SAID AS A MAP RATHER THAN AS A SENTENCE. This replaces the
      typographic sectors list that used to sit here: the client asked for
      sectors removed from the site completely (11/08/26), and the honest
      answer to "do you come to me" was always the geography, not a list of
      industries.

      The site's own component, the same one that closes /about/ and every
      service page, so the coverage claim is made in one place and one way.
      Without GOOGLE_MAPS_API_KEY it falls back to the keyless embed and still
      draws the South East; the key only buys the shaded counties.
    #}
    {% include "coverage-band.njk" %}

    {# ===================== 6 — SELECTED WORK =====================
      ONE case study at full size, not three at card size. A grid of three
      teases three jobs and proves none of them; one told properly, with the
      scope and the years on it, is the only section on the page that is
      evidence rather than claim.
    #}
    <section class="section section--paper hp-work" id="selected-work" aria-labelledby="hp-work-title">
      <div class="container">
        <p class="eyebrow hp-eyebrow"><span class="hp-eyebrow__num">02</span>Selected work</p>
        <h2 class="statement hp-work__title" id="hp-work-title">Cowdray Estate, <span class="em">a private supply serving multiple properties.</span></h2>

        {# STAND-IN, and a real photograph of the right place: Cowdray is named in
           the heading above, so unlike the anonymised studies this one can show
           the actual estate. It is not our plant, though, so the alt text says
           what the picture IS rather than claiming it is the installation.
           Credited on /credits/ (see _data/imageCredits.js). #}
        <figure class="hp-work__media parallax">
          <img src="/assets/case-cowdray.webp" alt="The ruins of Cowdray House across the parkland of the Cowdray Estate, West Sussex." width="1600" height="900" loading="lazy" decoding="async">
        </figure>

        <dl class="hp-work__meta">
          <div>
            <dt>Client</dt>
            <dd>Cowdray Estate</dd>
          </div>
          <div>
            <dt>Scope</dt>
            <dd>Private water supply, design and install, ongoing servicing</dd>
          </div>
          <div>
            <dt>Year</dt>
            <dd>2023 to present</dd>
          </div>
        </dl>

        {# Three Tier 2 terms wanted marking up here and the rule is two per
           paragraph, so pH correction is spelled out inline instead. #}
        <p class="hp-work__body">Cowdray needed a reliable, high-volume private supply serving multiple properties and estate operations. We surveyed two potential borehole sites, specified the higher-yield source, and installed a treatment system built around iron and manganese reduction, pH correction to stop the water corroding pipework, and UV sterilisation. The system has run continuously since 2023 with only scheduled maintenance, and passes annual UKAS-accredited testing every year.</p>

        {# Pointed at the private water supplies page, not at /case-studies/.
           The standalone index was retired and 301s here anyway, so linking to
           it only bought a redirect hop; and this IS where the work is, since
           case studies now live as carousels on the service pages. Cowdray is
           a private supply, so the reader lands among its neighbours. #}
        <p class="hp-work__link">
          <a class="btn-line" href="/services/private-water-supplies/">See more of our work <span class="btn-line__arrow" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 5l7 7-7 7"/></svg></span></a>
        </p>
      </div>
    </section>

    {# ===================== 7 — CREDENTIALS =====================
      THE QUALIFYING MOMENT. Everything above is what we do; this is the answer
      to "can you actually be put on our approved list". The question stays
      stuck to the top of the screen while the six answers scroll past it,
      because they are a list you scan rather than read.

      A description list, not headings: these are terms and their definitions,
      and keeping them out of the heading outline leaves the page with one H1
      and one H2 per section, which is what it should have.
    #}
    <section class="section hp-creds" id="credentials" aria-labelledby="hp-creds-title">
      <div class="container hp-creds__grid">
        <div class="hp-creds__aside">
          <p class="eyebrow hp-eyebrow"><span class="hp-eyebrow__num">03</span>Credentials</p>
          <h2 class="statement hp-creds__title" id="hp-creds-title">The paperwork clients ask about.</h2>
        </div>

        <dl class="hp-creds__list">
          <div class="hp-cred">
            <dt class="hp-cred__term">CHAS accredited</dt>
            <dd class="hp-cred__desc">For contractors and facilities managers who need us on their approved list.</dd>
          </div>
          <div class="hp-cred">
            <dt class="hp-cred__term">UKAS-accredited laboratory testing</dt>
            <dd class="hp-cred__desc">All water analysis goes through a UKAS-accredited lab, so your compliance evidence is current and defensible.</dd>
          </div>
          <div class="hp-cred">
            <dt class="hp-cred__term">Twenty years of legislation experience</dt>
            <dd class="hp-cred__desc">DWI, HSE, Environment Agency, Legionella regulations. We know the rules that will catch you out if you get it wrong.</dd>
          </div>
          <div class="hp-cred">
            <dt class="hp-cred__term">Section 80 notice? Speak to us first.</dt>
            <dd class="hp-cred__desc">We deal with private supply notices regularly. Full water analysis, remedial plan and liaison, so you meet the requirements without over-specifying.</dd>
          </div>
          <div class="hp-cred">
            <dt class="hp-cred__term">Fully insured</dt>
            <dd class="hp-cred__desc">Public liability and professional indemnity to a level suitable for large commercial and estate work.</dd>
          </div>
          <div class="hp-cred">
            <dt class="hp-cred__term">Direct engineer access</dt>
            <dd class="hp-cred__desc">Same phone number, same team. No call centre between you and the person who knows your system.</dd>
          </div>
        </dl>
      </div>
    </section>

    {# ===================== 8 — FILMSTRIP =====================
      THE EXHALE BEFORE THE ASK. Everything above it is being read; this is only
      looked at. It also does a job no paragraph on the page can: it shows that
      the plant rooms, the boreholes and the vans are ours, photographed on real
      jobs, which is the difference between a firm that installs seven-figure
      systems and a firm that has a website about them.

      The site's own component, carrying straight on from the navy above it, the
      same way it does on /about/. Drag or swipe; it works as a snapping scroller
      with no JavaScript at all.

      THE DEDICATED EMERGENCY BAND THAT USED TO SIT HERE IS GONE. The number is
      already persistent: the sticky bar carries it on every screen of every
      page, the hero states it in full, the footer repeats it, and on a phone the
      floating dock keeps it a thumb away. An 88px billboard of a number you
      cannot scroll away from anyway was the one section on this page that said
      something already said four times.
    #}
    {% set strip = [
      { image: "/assets/step-survey.webp",
        alt: "A country house and garden buildings across a lawn on a private estate, the kind of property surveyed before a supply is specified." },
      { image: "/assets/service-water-hygiene.webp",
        alt: "Labelled sample bottles from a day's testing lined up on a laboratory bench beside the results sheet." },
      { image: "/assets/step-design.webp",
        alt: "Architectural floor-plan and elevation drawings with a pen and brass ruler." },
      { image: "/assets/step-treatment.webp",
        alt: "A garage plant room with a brine tank, pressure filter vessels and a UV steriliser installed against the wall." },
      { image: "/assets/step-install.webp",
        alt: "An installed plant room with a storage tank, booster pumps, cylinder and a control panel wired and commissioned." },
      { image: "/assets/step-service.webp",
        alt: "An engineer's hand turning a brass gate valve on copper pipework, with a pressure gauge behind." },
      { image: "/assets/service-servicing.webp",
        alt: "An Invicta van on the drive of a period property, with the borehole chamber open and the pump and rising main lifted out." }
    ] %}
    {% include "filmstrip.njk" %}

    {# ===================== 9 — ENQUIRY =====================
      The exit door, and deliberately the quietest section on the page. The
      footer's own CTA is switched off in the front matter above so this is
      said once. "Send an enquiry" opens the site's enquiry drawer (enquiry.js
      intercepts a[href="/enquiry/"]); without JavaScript it redirects to the
      contact page, which carries the same form.
    #}
    <section class="section section--light hp-enquiry" id="enquiry-direct" aria-labelledby="hp-enquiry-title">
      <div class="container">
        <div class="hp-enquiry__inner">
          <p class="eyebrow hp-eyebrow"><span class="hp-eyebrow__num">04</span>Get in touch</p>
          <h2 class="statement hp-enquiry__title" id="hp-enquiry-title">Start with a conversation <span class="em">about your source.</span></h2>
          <p class="hp-enquiry__body">Whether you are planning a new supply, taking on a property with an existing one, or need someone to take over a system that has been neglected, we would be glad to visit and advise.</p>
          <div class="hp-enquiry__actions">
            <a class="btn-inquiry btn-inquiry--lg" href="/enquiry/">Send an enquiry <span class="btn-inquiry__arrow" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg></span></a>
            <a class="btn-line btn-line--lg" href="tel:{{ site.tel }}">Call {{ site.phone }} <span class="btn-line__arrow" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 5l7 7-7 7"/></svg></span></a>
          </div>
        </div>
      </div>
    </section>

    {# base.njk owns the shared script block and is not touched by this task,
       so the new component loads from the page that adopts it. Cache-busted by
       content with the same `v` filter every other asset on the site uses. #}
