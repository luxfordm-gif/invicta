    {% include "breadcrumb.njk" %}

    <!-- ===================== HERO ===================== -->
    <section class="hero">
      <div class="container hero__inner">
        <p class="hero__eyebrow">{{ heroEyebrow }}</p>
        <h1 class="hero__title">{{ heroTitle | safe }}</h1>
        <div class="hero__scroll" aria-hidden="true">
          <svg class="hero__scroll-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 4v15M6 13l6 6 6-6"/></svg>
        </div>
        <div class="hero__lead">
          <p class="hero__lead-lg">{{ heroLead | safe }}</p>
          {% if heroLeadSm %}<p class="hero__lead-sm">{{ heroLeadSm | safe }}</p>{% endif %}
        </div>
      </div>
    </section>

    <!-- ===================== INTRO (light) ===================== -->
    {% if introTitle %}
    <section class="section section--light service-intro" aria-labelledby="intro-title">
      <div class="container">
        <p class="eyebrow">{{ introEyebrow or "Overview" }}</p>
        <div class="rule"></div>
        <h2 id="intro-title" class="statement">{{ introTitle | safe }}</h2>
        {% if introBody %}
        <div class="service-intro__body">
          {% for p in introBody %}<p>{{ p | safe }}</p>{% endfor %}
        </div>
        {% endif %}
      </div>
    </section>
    {% endif %}

    <!-- ===================== WHAT'S INVOLVED (dark) ===================== -->
    {% if points %}
    <section class="section service-points" aria-labelledby="points-title">
      <div class="container service-points__grid">
        <aside class="service-points__aside">
          <p class="eyebrow">{{ pointsEyebrow or "What's involved" }}</p>
          <h2 id="points-title" class="statement">{{ pointsTitle | safe }}</h2>
          {% if pointsLead %}<p class="service-points__lead">{{ pointsLead | safe }}</p>{% endif %}
        </aside>
        <ul class="service-checklist">
          {% for pt in points %}
          <li class="service-check">
            <svg class="service-check__tick" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 6 9 17l-5-5"/></svg>
            <div><span class="service-check__title">{{ pt.title }}</span>{% if pt.desc %}<span class="service-check__desc">{{ pt.desc | safe }}</span>{% endif %}</div>
          </li>
          {% endfor %}
        </ul>
      </div>
    </section>
    {% endif %}

    <!-- ===================== CASE STUDY (light, optional) ===================== -->
    {% if caseStudy %}
    <section class="section section--light service-case" aria-labelledby="case-title">
      <div class="container">
        <p class="eyebrow">Case study</p>
        <div class="rule"></div>
        <h2 id="case-title" class="statement service-case__title">{{ caseStudy.title | safe }}</h2>
        <div class="service-case__grid">
          <div class="service-case__body">
            {% for p in caseStudy.body %}<p>{{ p | safe }}</p>{% endfor %}
          </div>
          {% if caseStudy.highlight %}
          <aside class="service-case__aside">
            <p class="service-case__highlight">{{ caseStudy.highlight | safe }}</p>
            {% if caseStudy.client %}<p class="service-case__client">{{ caseStudy.client }}</p>{% endif %}
          </aside>
          {% endif %}
        </div>
      </div>
    </section>
    {% endif %}

    <!-- ===================== FAQ (dark) ===================== -->
    {% if faqs %}
    <section class="section faq" aria-labelledby="faq-title">
      <div class="container faq__grid">
        <aside class="faq__aside">
          <span class="eyebrow">Common questions</span>
          <h2 id="faq-title" class="faq__title">{{ faqTitle or "Frequently asked." }}</h2>
          {% if faqIntro %}<p class="faq__intro">{{ faqIntro }}</p>{% endif %}
        </aside>
        <div class="faq__list">
          {% for f in faqs %}
          <details class="faq__item">
            <summary class="faq__q">
              {{ f.q }}
              <svg class="faq__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><path d="M12 5v14M5 12h14"/></svg>
            </summary>
            <p class="faq__a">{{ f.a | safe }}</p>
          </details>
          {% endfor %}
        </div>
      </div>
    </section>
    {% endif %}
